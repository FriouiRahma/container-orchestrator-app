name: Demo Env Secrets
on:
  # push:
    # branches:
    #   - main
 workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: echo "Hello, World!"

      - name: Install
        env: 
          APP_ENV: demo
        run: echo "Environment $APP_ENV"